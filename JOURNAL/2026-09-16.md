# 16 September 2026
**Day-07**

* Today I started practicing C programming as part of my AI and robotics learning journey.

* I learned how to declare and use basic C data types: an `int` for a battery level, a `float` for speed, and a `char` for a status code.

* I used `scanf` to take the battery level as input from the user, then displayed the stored values with `printf`.

* I practiced an `if` and `else` condition to make a simple battery-monitoring decision: show a low-battery warning below 20, otherwise confirm that the battery level is sufficient.

* This exercise helped me see how C can be used for the kind of simple, direct control logic that is common in embedded systems and robotics.
