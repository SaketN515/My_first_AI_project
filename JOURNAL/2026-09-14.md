# 14 September 2026
**Day-05**

* Today I learned how to use loops with a list to check for a specific object, like a person detected by a robot or an AI camera.

* I practiced using a for loop to go through every object in the list and use an if condition to check whether the object is a "person".

* I also learned how to use range(len(objects)) when I need both the position of the object and its value in the list.

* Using f-string, I printed each object with its index, which made it easier to understand how the loop is moving through the list one by one.

* This feels like a small version of object detection logic, where an AI system checks what it can see and reacts when it finds something important.

* It was simple practice, but I can see how loops and conditions will be very useful when I start making more realistic AI agent projects.
