# 18 September 2026
**Day-09**

* Today I continued my C programming practice and focused on combining arrays, strings, conditions, loops, and structures in one robot-related exercise.

* I worked on a small program that stored the robot’s name, status, battery level, and object list, then used these values to make simple decisions about the robot’s situation.

* I practiced using arrays of strings to represent objects like chair, person, and bottle, and I scanned through them with a for loop to detect whether a person was present.

* I also used `strcmp` to compare string values, which helped the program decide whether the robot was in a Ready state and whether a target object had been detected.

* I explored pointer usage by creating a pointer to the distance array and using pointer arithmetic to read values in memory and check whether an obstacle was near.

* I used a `struct` to model a robot with fields such as battery, speed, and status, then printed those values to verify that the structure was storing the correct data.

* I also connected the logic to real robot behavior: if the battery was low, the system showed a warning; if the robot found a person, it reported detection; otherwise, it continued scanning.

* This session helped me connect several important C concepts into a single practical example, and it made the relationship between data storage, decision-making, and robot control much clearer.

* I can see that C is useful for embedded systems and robotics because it allows direct control over memory, values, and conditions in a very efficient way.
